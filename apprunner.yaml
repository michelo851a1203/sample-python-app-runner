version: 1.0.0
runtime: python311

build:
  commands:
    pre-build:
      - echo "Running pre-build..."
    build:
      - pip3 install uv
      - uv sync

run:
  runtime-version: 3.11
  command: uv run uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080

