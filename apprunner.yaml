version: 1.0.0
runtime: python311

build:
  commands:
    pre-build:
      - echo "Running pre-build..."
    build:
      - pip3 install -r requirements.txt

run:
  runtime-version: 3.11.13
  command:
    command: python3 main.py
    network:
      port: 8080

