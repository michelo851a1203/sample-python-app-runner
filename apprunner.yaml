version: 1.0.0
runtime: python311

build:
  commands:
    pre-build:
      - echo "Running pre-build..."
    build:
      - pip install -r requirements.txt

run:
  command:
    command: python main.py
    network:
      port: 8080

